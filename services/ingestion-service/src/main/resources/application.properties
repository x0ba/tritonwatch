spring.application.name=ingestion-service
spring.config.import=optional:file:../../.env[.properties],optional:file:./.env[.properties]
server.port=${SERVER_PORT:8083}
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/ingestion_service}
spring.datasource.username=${DATABASE_USERNAME:ingestion_service}
spring.datasource.password=${DATABASE_PASSWORD:ingestion_service}
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.open-in-view=false
spring.flyway.enabled=true
spring.kafka.bootstrap-servers=${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
spring.kafka.producer.key-serializer=org.apache.kafka.common.serialization.StringSerializer
spring.kafka.producer.value-serializer=org.springframework.kafka.support.serializer.JacksonJsonSerializer
spring.kafka.producer.acks=all
spring.kafka.producer.properties.enable.idempotence=true
spring.kafka.consumer.group-id=ingestion-service
spring.kafka.consumer.auto-offset-reset=earliest
spring.kafka.consumer.enable-auto-commit=false
spring.kafka.consumer.key-deserializer=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.consumer.value-deserializer=org.springframework.kafka.support.serializer.JacksonJsonDeserializer
spring.kafka.consumer.properties.spring.json.trusted.packages=app.tritonwatch.contracts.event
spring.kafka.listener.ack-mode=record
tritonwatch.ingestion.poll-interval=${INGESTION_POLL_INTERVAL:2m}
tritonwatch.ucsd-api.base-url=${UCSD_API_BASE_URL:https://classplanner.apps.ucsd.edu}
tritonwatch.catalog.sync-cron=${CATALOG_SYNC_CRON:0 0 5 * * MON}
tritonwatch.catalog.sync-on-startup=${CATALOG_SYNC_ON_STARTUP:true}
tritonwatch.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:5173}
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.probes.enabled=true
outbox.poll-delay-ms=${OUTBOX_POLL_DELAY_MS:1000}
outbox.batch-size=${OUTBOX_BATCH_SIZE:50}
outbox.send-timeout-seconds=${OUTBOX_SEND_TIMEOUT_SECONDS:10}
