info:
  name: term api
  type: http
  seq: 2

http:
  method: GET
  url: https://classplanner.apps.ucsd.edu/api/v1/planner/terms
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
