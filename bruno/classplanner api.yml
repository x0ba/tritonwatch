info:
  name: classplanner api
  type: http
  seq: 1

http:
  method: POST
  url: https://classplanner.apps.ucsd.edu/api/v1/catalog/courses/search
  body:
    type: json
    data: |-
      {
        "term_code": "FA26",
        "course_key": [
          "CSE 100",
          "CSE 008A",
          "MATH 020C"
        ],
        "limit": 48
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
